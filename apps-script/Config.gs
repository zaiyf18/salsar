/**
 * سلسار V2 — Config.gs
 * =====================================
 * الإعدادات والثوابت. لا منطق هنا — فقط قيم يعتمد عليها باقي النظام.
 * لتغيير أي معرّف (شيت أو مجلد)، عدّل هنا فقط.
 */

const SALSAR = Object.freeze({

  // ---- الشيت ----
  SPREADSHEET_ID: 'ضع-هنا-معرّف-جدولك',

  // ---- المنطقة الزمنية والتنسيقات ----
  TIME_ZONE: 'Asia/Riyadh',
  DATE_FORMAT: 'yyyy-MM-dd',
  DATETIME_FORMAT: 'yyyy-MM-dd HH:mm',

  // ---- ثابت المستودع (الحائز الافتراضي) ----
  WAREHOUSE_ID: 'WAREHOUSE',
  WAREHOUSE_NAME: 'أمين المستودع',

  // ---- مدة الكاش الافتراضية بالثواني ----
  // v2.5: رُفعت من 60 إلى 1800 (30 دقيقة) بعد قياس أثبت أن bootstrap يكلّف
  // 10.4 ثانية بلا كاش مقابل 1.5 ثانية معه. المهلة القصيرة كانت بلا فائدة تقريبًا
  // لأن كل كتابة تستدعي invalidateSheet_ صراحةً (11 موضعًا، وداخل append_ و update_)،
  // فالمهلة لا تحمي إلا من تعديل الشيت باليد — وهو نادر عند هذا المستخدم.
  CACHE_TTL_SECONDS: 1800,

  // ---- أسماء الأوراق (14 ورقة حسب مخطط v2) ----
  SHEETS: {
    vehicles:              'المركبات',
    drivers:               'السائقون',
    configurations:        'سجل تجهيزات المركبة',
    checklist:             'بنود التشييك',
    checklistTranslations: 'ترجمات بنود التشييك',
    assetTranslations:     'ترجمات الأصول',
    handovers:             'محاضر العهدة',
    results:               'نتائج التشييك',
    transfers:             'نقل الأصول',
    documents:             'وثائق المركبات',
    certifiedRecords:      'المحاضر المصادقة',
    operationImages:       'صور العمليات',
    translations:          'الترجمات',
    audit:                 'سجل التدقيق',
    settings:              'الإعدادات'
  },

  // ---- معرّفات مجلدات Drive ----
  FOLDERS: {
    handovers: 'ضع-هنا-معرّف-مجلد-محاضر-العهدة',
    transfers: 'ضع-هنا-معرّف-مجلد-محاضر-نقل-الأصول',
    images:    'ضع-هنا-معرّف-مجلد-صور-العمليات'
  }
});

// فئات المركبات المعتمدة (كانت متفرقة في Models.gs — الآن في مكان واحد)
const SALSAR_VEHICLE_CATEGORIES = Object.freeze([
  'شاحنة خزان مياه',
  'شاحنة صندوق مغلق',
  'شاحنة صندوق مفتوح',
  'شاحنة صندوق تبريد',
  'بكب صغيرة',
  'خاصة',
  'أخرى'
]);

/**
 * البريد المصرَّح له بالتنفيذ المباشر (من محرر Apps Script).
 * الوسيط على الاستضافة يستخدم SALSAR_API_TOKEN (في Script Properties) بدلًا من هذا.
 * لتغييره: بدّل السطر أدناه ثم احفظ.
 */
function getAuthorizedEmail_() {
  return 'you@example.com';
}
