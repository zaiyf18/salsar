# سجل الإصدارات

الأحدث أولًا. التواريخ بتوقيت الرياض.

## 2.5.5 — 2026-09-10
**شريط التقدّم صار يعني ما يقوله.** كان يعرض «0 من 1 (100%)»: النسبة تقيس بايتات غادرت المتصفح، والعدّاد يقيس صورًا أكّد الخادم حفظها، وبينهما ثوانٍ ينتظر فيها ردّ Apps Script. الآن الإرسال 90% من الشريط والتأكيد آخر 10%، وتظهر «بانتظار تأكيد الخادم…» في الفجوة. يشمل رفع الصور ورفع المحضر المصادق عليه.
`live-api.js` · `app-template.html`

## 2.5.4 — 2026-09-10
**رقم الترجمة يُعرَض مجموعتين** `1234 5678`. العرض وحده تغيّر؛ المُشتقّ والمُخزَّن والمُرسَل ثمانية أرقام متصلة كما كان، فلا رقم مطبوع سابقًا يبطل. وأُضيف `dir="ltr"` لحقل الإدخال — بدونه يقلب التنسيق ثنائي الاتجاه المجموعتين فتُقرأ `5678 1234`.
`app-template.html` · `translation-code.html`

## 2.5.3 — 2026-09-10
**شارة حمراء «غير مصادق N»** على بطاقة المركبة وبطاقة السائق، تحتسب محاضر العهدة ونقل الأصول معًا ولطرفَي المحضر. شارة إضافية لا تحجب شارة العهدة.
`app-template.html`

## 2.5.2 — 2026-09-10
**إصلاح اختفاء الملاحظات الموروثة** من المحضر المطبوع والملحق الكامل — أخطر خلل ظهر في المشروع. كانت `receiptChecklist` تفترض أن أي بند غائب عن لقطة المحضر حالته «لا ملاحظات»، وورقة النتائج لا تسجّل إلا الفروقات؛ فبند حالته «لا ينطبق» موروثة من محضر سابق كان يُعرض على المحرّر بملاحظته ويُطبع بلا ملاحظة — أي أن الملحق كان **يثبت عكس الحقيقة**. أُضيفت `checklistStateAt` تعيد بناء الحالة القائمة بإعادة تشغيل الفروقات، وأُعيدت كتابة `receiptExceptions` بقاعدة واحدة مطابقة لشاشة الإنشاء.
`app-template.html` · `live-api.js`

## 2.5.1 — 2026-09-10
إعادة محاولة واحدة عند فشل **وصول الرد** فقط، ومشروطة بأمان الإعادة (فعل قراءة، أو كتابة تحمل `clientRequestId`). وتحسين تحديث الكاش بلا إبطال مسبق.
`live-api.js` · `WebApp.gs` · `Sheets.gs`

## 2.5 — 2026-09-10
**علاج البطء، بقياس لا بتخمين.** `testCachePerformance()` = 10,432 مللي ثانية بلا كاش مقابل 1,487 معه. السبب: مهلتا الكاش 60 ثانية بينما كل كتابة تُبطل الكاش صراحةً (11 موضعًا) — فالمهلة القصيرة لا تشتري طزاجة وتكلّف إعادة بناء كاملة كل دقيقة. رُفعت إلى 1800 و600، وأُضيف مشغّل تدفئة كل 15 دقيقة. **القياس بعد: 7.04 ثانية ← 367 مللي ثانية (19×)، والصفحة 8.42 ← 1.30 ثانية.**
`Config.gs` · `Sheets.gs` · `WebApp.gs` · `fleet-data.php`

## 2.4 — 2026-09-10
ترجمة محضر نقل الأصول إلى خمس لغات، وكثافة عرض متكيّفة بعد قياس أن اثني عشر أصلًا آخر ما تسعه صفحة A4.
`app-template.html` · `translation.html` · `fleet-data.php` · ورقة «ترجمات الأصول»

## 2.3.5 — 2026-09-09
**صفحة زائدة في الطباعة.** `min-height:285mm` يساوي الارتفاع القابل للطباعة عند هامش 6 مم بلا أي احتمال — فمحضر من ثلاثة أقسام صار ست صفحات عند هامش 10 مم. خُفض إلى 262 مم بعد قياس فعلي بـ`Chromium`.
`app-template.html`

## 2.3.4 — 2026-09-09
**عدّاد «المركبات المسلّمة» = صفر** مع وجود محاضر معتمدة. السبب: الشدّة — `'مسلّمة' !== 'مسلمة'` — و`sourceData.vehicleStatuses` لم يكن موجودًا أصلًا. العدّاد صار 5.
`app-template.html`

## 2.3.3 — 2026-09-09
**«هذا الحساب غير مصرح له» عشوائيًا.** عيب بنيوي: `authorize_` كان يسقط إلى مسار لا ينجح أبدًا تحت `ANYONE_ANONYMOUS`، فيجمع ثلاثة أعطال مختلفة في رسالة واحدة مضلّلة. فُصلت إلى أربعة رموز، وأُضيفت إعادة محاولة في PHP آمنة لأن `authorize_` يسبق `dispatch_` — فالرفض لم يكتب شيئًا.
`WebApp.gs` · `bootstrap.php`

## 2.3.2 — 2026-09-09
حذف دالة طباعة ميتة كانت تحمل **نص إقرار ثالثًا مخالفًا للنص المعتمد**. لم تكن مربوطة بأي زر، لكن وجودها خطر: من يعدّلها يظن أنه عدّل المحضر.
`app-template.html`

## 2.3 — 2026-09-09
نقل `Hostinger/salsar-private/` خارج جذر الويب. رفع الصور بـ`XMLHttpRequest` مع شريط تقدّم حقيقي.
